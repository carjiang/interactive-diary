services:
  app:
    build: .
    container_name: interactive-diary
    command: sleep infinity  
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    restart: unless-stopped
    stdin_open: true      # -i